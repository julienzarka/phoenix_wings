name: phoenix_wings
description: Phoenix Channel client for Dart
version: 0.2.1
author: mfeckie<mfeckie@gmail.com>
homepage: https://github.com/mfeckie/phoenix_wings

dev_dependencies:
  dartdoc: "0.28.3+2"
  test: "1.6.2"
  mockito: "^3.0.0"
  stream_channel: "2.0.0"
  intl: "^0.15.6"

dependencies:
  flutter:
    sdk: flutter

environment:
  sdk: '>=2.2.0 <3.0.0'

# For information on the generic Dart part of this file, see the
# following page: https://www.dartlang.org/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The androidPackage and pluginClass identifiers should not ordinarily
  # be modified. They are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    androidPackage: de.movementfam.webapp.background_service
    pluginClass: BackgroundServicePlugin